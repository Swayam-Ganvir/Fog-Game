# --- 1. Security (CRITICAL) ---
# These files contain your MongoDB URI and API Keys.
# If you upload these, hackers can steal your database.
.env
.env.local
.env.production
.env.test
.env.*

# --- 2. Dependencies (Heavy Files) ---
# Netlify and Render will install these automatically.
# Uploading them will break the deployment.
node_modules/
package-lock.json  # Optional: Keep this if you want exact versions, but remove if you get errors.

# --- 3. Build Outputs ---
# These are the results of 'npm run build'.
# Netlify creates its own version on their servers.
.next/
dist/
build/
out/

# --- 4. Logs and System Files ---
npm-debug.log*
yarn-debug.log*
logs/
*.log
.DS_Store
Thumbs.db
.vscode/